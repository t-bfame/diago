syntax = "proto3";

option go_package = "proto-gen/lol";

service RouteGuide {
    rpc RouteChat(stream Point) returns (stream Point) {}
}

message Point {
    int32 latitude = 1;
    int32 longitude = 2;
}